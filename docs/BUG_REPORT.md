# Bug Report

| ID | Priority | Area | Status | Repro | Fix |
|---|---|---|---|---|---|
| BUG-001 | P0 | Repository | Fixed | Workspace was empty. | Created V1 foundation, docs, apps, APIs, scripts, and tests. |
| BUG-002 | P0-Pay | Billing | Open until production | Real Creem checkout cannot be verified without credentials. | Local mock route exists; production test deferred to final joint deployment stage. |
| BUG-003 | P0-Auth | Auth | Open until production | Supabase/Google OAuth cannot be verified without credentials. | Auth flow and API placeholders exist; production setup docs created. |
| BUG-004 | P0-Pay | Billing | Fixed locally | Checkout previously returned placeholder production URL. | Added Creem `/v1/checkouts` call with server-only API key and product IDs. |
| BUG-005 | P0-Pay | Webhook | Fixed locally | Webhook used in-memory idempotency and local secret fallback. | Production now requires signature secret and Supabase `webhook_events` persistence. |
| BUG-006 | P0-Credits | Extensions | Fixed locally | AI buttons produced direct mock output. | AI actions now call `/api/ai/run-task` before returning results. |
| BUG-007 | P1-Release | Extensions | Fixed locally | Extension source hardcoded localhost as app base URL. | Build script now injects `AUTOPUB_APP_BASE_URL` / `APP_BASE_URL`, defaulting to local only for development. |
| BUG-008 | P1-UX | Pricing | Fixed locally | Pricing page looked like an unfinished placeholder and did not explain what AutoPub is. | Rebuilt pricing page with product positioning, plan value, extension matrix benefits, payment status, FAQ notes, and responsive layout. |
| BUG-009 | P1-Scope | Pricing | Fixed locally | Pro Plus copy implied a path or entitlement into the V2 SEO/GEO SaaS. | Clarified that Pro/Pro Plus cover V1 plugin features only; V2 hosted SEO/GEO automation and publishing SaaS is separate. |
| BUG-010 | P1-Architecture | Extensions | Fixed locally | Extension popup code duplicated app URL, active-tab lookup, AI API calls, and upgrade navigation. | Added `packages/extension/core.js`, injected it into every built extension, and enforced usage in lint. |
| BUG-011 | P1-Website | Official site | Fixed locally | Website pages looked like placeholders and did not explain product value, customer problem, SaaS demand, timeline, blog structure, or pricing boundary. | Rebuilt home, SaaS waitlist, content hub, plugin blog, SaaS blog, FAQ, and pricing notes with clearer product narrative and validation flow. |
| BUG-012 | P1-Website | Official site | Fixed locally | Website still used too much abstract SEO/GEO wording and did not explain the final SaaS in plain language. | Rewrote core pages in plain Chinese: AutoPub will ultimately help websites automatically plan articles, write content, generate images, publish, and keep optimizing to get more customers. Added a more technology-oriented visual system. |
| BUG-013 | P0-Credits | AI/Credits | Fixed locally, production DB pending | AI credits were only deducted from an in-memory user object. | Added production Supabase RPC path for atomic AI credit spend, `credit_logs`, and `ai_tasks` completion records; documented required SQL functions. |
| BUG-014 | P1-Website | Waitlist | Fixed locally, production DB pending | Waitlist API only echoed successful submissions. | Added `waitlist_entries` Supabase persistence path when `MOCK_MODE=false`. |
| BUG-015 | P1-Release | Extension packaging | Fixed locally | Chrome Store zips could be generated with localhost app URLs. | Packaging now refuses release builds unless `AUTOPUB_APP_BASE_URL` or `APP_BASE_URL` is production; local QA requires `AUTOPUB_ALLOW_LOCAL_RELEASE=true`. |
| BUG-016 | P1-Extension | Competitor Monitor | Fixed locally | Snapshot storage used string-built JSON and could fail for quoted URLs. | Replaced string-built JSON with direct object assignment before `chrome.storage.local.set`. |
| BUG-017 | P1-Billing | Creem webhook | Fixed locally, production DB pending | Webhook recorded idempotency after entitlement sync and could grant credits for non-paid statuses. | Webhook now records event before sync and only grants credits for active/paid/completed/trialing statuses. |
| BUG-018 | P1-Auth | Web auth | Fixed locally, production Supabase pending | Login/register pages only used alerts. | Added `/api/auth/login` and `/api/auth/register`, wired forms to Supabase Auth in production and mock sessions locally. |
| BUG-019 | P0-Release | Mock mode | Fixed locally | Production code defaulted to mock unless `MOCK_MODE=false` was set. | Mock mode is now enabled only by explicit `MOCK_MODE=true`; dev scripts set it for local use, and production packaging requires `MOCK_MODE=false`. |
| BUG-020 | P0-Billing | Creem webhook | Fixed locally, production DB pending | Webhook idempotency and entitlement sync were not one atomic database operation. | Added `process_creem_webhook` Supabase RPC path and switched production webhook handler to the RPC; SQL also documents AI failure credit refunds. |
| BUG-021 | P1-Auth | Extension login bridge | Fixed locally, live flow pending | Web login token stayed in website `localStorage` while extensions read `chrome.storage.local`. | Added Sign In buttons to extension popups and content-script session sync from AutoPub web login/register pages into extension storage. |
